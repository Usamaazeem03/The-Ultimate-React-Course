# 🧠 **React Concepts I Used**

1. **State Management (`useState`)** – to open and close each accordion item.
2. **Props** – to pass data (`title`, `text`, `num`) from parent to child components.
3. **Conditional Rendering** – to show or hide the content box based on state.
4. **Dynamic Rendering (Array Mapping)** – to render multiple FAQ items using `map()`.
5. **Component Reusability** – by creating separate components (`Accordion`, `AccordionItem`).
6. **Event Handling (onClick)** – to toggle the accordion open or closed.
7. **Key Prop** – to uniquely identify each item in the list for efficient rendering.
